


/**
 * @param {!polina.Packet} header
 * @param {!polina.Cursor} cursor
 * @param {!Buffer} chunk
 * @param {!Array.<*>} options
 * @return {boolean}
 * @constructor
 */
polina.redis.resp.HEADER_PARSER = function(header, cursor, chunk, options) {

};


/**
 * @param {!polina.Packet} body
 * @param {!polina.Cursor} cursor
 * @param {!Buffer} chunk
 * @param {!Array.<*>} options
 * @return {boolean}
 * @constructor
 */
polina.redis.resp.BODY_PARSER = function(body, cursor, chunk, options) {

};


/**
 * @param {!polina.Packet} result
 * @param {!polina.Cursor} cursor
 * @param {!Buffer} chunk
 * @param {!Array.<*>} options
 * @return {boolean}
 * @constructor
 */
polina.redis.resp.RESULT_PARSER = function(result, cursor, chunk, options) {

};